# Handoff Snapshots

Use this directory for dated handoff snapshots when a session is interrupted and the active `.codex/HANDOFF.md` is not enough.

Naming convention:

```text
YYYY-MM-DD-short-topic.md
```

Each snapshot should include goal, done work, in-progress state, changed files, verification, blockers, and the next entry point.
